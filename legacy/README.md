# legacy/

Самая ранняя версия проекта (чтение CAN-шины через `python-can`,
`src/main.py`, `src/protocol.py`, `vcan/vcan.py`, карта параметров
`canmap.yaml`). Не используется и не импортируется текущим дашбордом
в `app/`; сохранена как историческая справка. Зависимости — в
`legacy/requirements.txt`.
